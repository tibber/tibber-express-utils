{
  "extends": "./node_modules/gts/tsconfig-google.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "dist",
    "allowSyntheticDefaultImports": true,
    "esModuleInterop": true,
    "lib": [
      "ES2023",
      "DOM",
      "DOM.Iterable",
      "ESNext"
    ]
  },
  "include": [
    "src/**/*",
    "test/**/*"
  ],
  "//exclude": "test/consumer/probe.ts imports 'tibber-express-utils' by package name and is compiled only inside the scratch consumer project that test/consumer/run.sh builds.",
  "exclude": [
    "test/consumer"
  ]
}
